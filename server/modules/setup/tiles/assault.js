class CapturePointArea {
    constructor (teamId, color, protector, isSpawn) {
        this.ProtectorTile;
        this.DecorativeTile;
        this.decorative = [];
    }
}

class TeamCapturePointAreaGroup {
    constructor (teamId, color = getTeamColor(teamId), protector, isSpawn) {
        this.
    }
    makeDominatorCapturePointArea () {}
    makeSanctuaryCapturePointArea () {}
}

new Tile({ init: tile => makeDefenderDominator(tile, TEAM_BLUE, TEAM_BLUE, TEAM_ENEMIES) });


module.exports = { TeamCapturePoints };